#  Sales Summary Report using MySQL and Python

This project connects to a MySQL database, retrieves basic sales data using SQL, and displays the results in tabular and graphical formats using Python libraries such as `pandas` and `matplotlib`.

##  Tools & Libraries Used

- **Python 3**
- **MySQL Server**
- `mysql-connector-python`
- `pandas`
- `matplotlib`
- **Jupyter Notebook**